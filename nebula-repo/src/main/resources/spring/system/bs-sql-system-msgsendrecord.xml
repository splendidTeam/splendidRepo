<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<bean id="MsgSendRecord.findAllMsgSendRecordList" class="loxia.dao.support.DynamicQueryHolder">
		<description></description>
		<constructor-arg>
			<value>
				<![CDATA[  
		       		select * from T_SYS_MSG_SEND_RECORD 
		       		
                 ]]>
			</value>
		</constructor-arg>
	</bean>	
	
	<bean id="MsgSendRecord.findAllNoFeedbackMsgSendRecordList" class="loxia.dao.support.DynamicQueryHolder">
		<description></description>
		<constructor-arg>
			<value>
				<![CDATA[  
		       		select * from T_SYS_MSG_SEND_RECORD 
		       		
		       		where FEEDBACK_TIME is  null order by id asc
                 ]]>
			</value>
		</constructor-arg>
	</bean>	
	
	
	
	<bean id="MsgSendRecord.findMsgSendRecordListByIds" class="loxia.dao.support.DynamicQueryHolder">
		<description></description>
		<constructor-arg>
			<value>
				<![CDATA[  
		       		select * from T_SYS_MSG_SEND_RECORD where 
		       		id in (
		       		#foreach( $id in $ids )
    					$id,
					 #end
		       		 -1)
		       		
                 ]]>
			</value>
		</constructor-arg>
	</bean>	
	
	
	<bean id="MsgSendRecord.findMsgSendRecordListByQueryMap" class="loxia.dao.support.DynamicQueryHolder">
		<description></description>
		<constructor-arg>
			<value>
				<![CDATA[  
		       		select * from T_SYS_MSG_SEND_RECORD where 
		       			1=1	
		       		
					
		       			
		       				#if($ifIdentify)
		       					and IF_IDENTIFY=:ifIdentify
		       				#end

		       			
		       				#if($targetId)
		       					and TARGET_ID=:targetId
		       				#end

		       			
		       				#if($sendTime)
		       					and SEND_TIME >= :sendTimeStart
		       				#end
		       				#if($sendTime)
		       					and SEND_TIME <= :sendTimeEnd
		       				#end

		       			
		       				#if($feedbackTime)
		       					and FEEDBACK_TIME >= :feedbackTimeStart
		       				#end
		       				#if($feedbackTime)
		       					and FEEDBACK_TIME <= :feedbackTimeEnd
		       				#end

		       			
		       				#if($sendCount)
		       					and SEND_COUNT=:sendCount
		       				#end

		       			
		       				#if($createTime)
		       					and CREATE_TIME >= :createTimeStart
		       				#end
		       				#if($createTime)
		       					and CREATE_TIME <= :createTimeEnd
		       				#end
		       				
                 ]]>
			</value>
		</constructor-arg>
	</bean>	
	
	<bean id="MsgSendRecord.findMsgSendRecordListByQueryMapWithPage" class="loxia.dao.support.DynamicQueryHolder">
		<description></description>
		<constructor-arg>
			<value>
				<![CDATA[  
		       		select * from T_SYS_MSG_SEND_RECORD where 
		       		
		       			1=1	
		       		
					
		       			
		       				#if($ifIdentify)
		       					and IF_IDENTIFY=:ifIdentify
		       				#end

		       			
		       				#if($targetId)
		       					and TARGET_ID=:targetId
		       				#end

		       			
		       				#if($sendTime)
		       					and SEND_TIME >= :sendTimeStart
		       				#end
		       				#if($sendTime)
		       					and SEND_TIME <= :sendTimeEnd
		       				#end

		       			
		       				#if($feedbackTime)
		       					and FEEDBACK_TIME >= :feedbackTimeStart
		       				#end
		       				#if($feedbackTime)
		       					and FEEDBACK_TIME <= :feedbackTimeEnd
		       				#end

		       			
		       				#if($sendCount)
		       					and SEND_COUNT=:sendCount
		       				#end

		       			
		       				#if($createTime)
		       					and CREATE_TIME >= :createTimeStart
		       				#end
		       				#if($createTime)
		       					and CREATE_TIME <= :createTimeEnd
		       				#end
		       				
                 ]]>
			</value>
		</constructor-arg>
	</bean>	
	
	
	<bean id="MsgSendRecord.findMsgSendRecordAndContentListByQueryMapWithPage" class="loxia.dao.support.DynamicQueryHolder">
		<description></description>
		<constructor-arg>
			<value>
				<![CDATA[  
		           select record.id as id,
					       record.create_time AS createTime,
					       record.ext as ext,
					       record.feedback_time as feedbackTime,
					       record.if_identify as ifIdentify,
					       record.send_count as sendCount,
					       record.send_time as sendTime,
					       record.target_id as targetId,
					       record.version as version,
					       content.msg_body as msgBody
					from T_SYS_MSG_SEND_RECORD record 
					     LEFT JOIN t_sys_msg_send_content content ON record.id=content.id 
                    where 
		       			 1=1	
		       				#if($ifIdentify)
		       					and record.if_identify  like :ifIdentify
		       				#end

		       				#if($targetId)
		       					and record.target_id =:targetId
		       				#end
		       			
		       				#if($sendTimeStart)
		       					and record.send_time >= :sendTimeStart
		       				#end
		       				#if($sendTimeEnd)
		       					and record.send_time <= :sendTimeEnd
		       				#end
		       			
		       				#if($feedbackTimeStart)
		       					and record.feedback_time >= :feedbackTimeStart
		       				#end
		       				#if($feedbackTimeEnd)
		       					and record.feedback_time <= :feedbackTimeEnd
		       				#end
		       			
		       				#if($sendCount)
		       					and record.send_count =:sendCount
		       				#end
                           
                            #if($msgBody)
		       					and content.msg_body like :msgBody
		       				#end
		       			
		       				#if($createTimeStart)
		       					and record.create_time >= :createTimeStart
		       				#end
		       				#if($createTimeEnd)
		       					and record.create_time <= :createTimeEnd
		       				#end
                 ]]>
			</value>
		</constructor-arg>
	</bean>	
</beans>